"""App configuration for About app."""
from django.apps import AppConfig


class AboutConfig(AppConfig):
    """About app configuration."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "about"
